/**
 * If user submits a new atm -- create a table that associates a foreign key with atm table
 * so we know which atm they submitted. 
 * 
 * Useful to flag/follow up ask questions/etc.
 * --> also avoid duplicate addresses
 */